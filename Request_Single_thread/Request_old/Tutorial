
requests包的使用

x = get(url, headers,...) #获得respond请求
x.text #解析原文的内容（默认编码）
x.content #原始的字节


使用Xpath

路径表示法
- //定位根节点
- text():提取文本内容
- /@attr:提取属性的内容

筛选条件
- /div[@id]
- /div[@id = "conebt_id"]
- /book[price > 100]


例子：
spans = doc.xpath('//span[@class = "rating_nums"]/text()') #返回打分值
images  = doc.xpath('//img/@src')




